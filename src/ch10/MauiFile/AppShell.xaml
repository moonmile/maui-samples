<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiFile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiFile"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
        <ShellContent
            Title="Create"
            ContentTemplate="{DataTemplate local:CreateFilePage}"
            Route="CreateFilePage" />
        <ShellContent
            Title="Read"
            ContentTemplate="{DataTemplate local:ReadFilePage}"
            Route="ReadFilePage" />
        <ShellContent
            Title="Log"
            ContentTemplate="{DataTemplate local:LogDataPage}"
            Route="LogDataPage" />
    </TabBar>

</Shell>
